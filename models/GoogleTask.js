export default class GoogleTask {

  constructor(id, title, notes, status, due, completed, url) {
    this.id = id;
    this.title = title;
    this.notes = notes;
    this.status = status;
    this.due = due;
    this.completed = completed;
    this.url = url;
  }

}